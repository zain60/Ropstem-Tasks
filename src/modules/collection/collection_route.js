import express from 'express';
const router = express.Router();
// import {getTokenmetaData, uploadNfts, uploadJason,testAuthentication,pinDirectoryToPinata ,pinJasonDirectoryToPinata} from '../metaData/metadata-service.js';

router.route('/createCollection').post(
    // uploadNfts
);
export default router;